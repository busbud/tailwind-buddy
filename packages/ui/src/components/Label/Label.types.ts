// export interface LabelBaseProps extends React.HTMLAttributes<HTMLBaseElement> {
//   /** The component used for the root node. Either a string to use a HTML element or a component. */
//   as?: React.ElementType;
//   disabled?: boolean;
// }
