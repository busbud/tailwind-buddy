{
  "name": "@busbud/tailwind-buddy",
  "version": "2.4.3",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "./dist/tailwindbuddy.umd.cjs",
  "module": "./dist/tailwindbuddy.js",
  "types": "./dist/main.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/main.d.ts",
        "default": "./dist/tailwindbuddy.js"
      },
      "require": {
        "types": "./dist/main.d.cts",
        "default": "./dist/tailwindbuddy.umd.cjs"
      }
    },
    "./dist/*": "./dist/*"
  },
  "scripts": {
    "build": "tsc && vite build",
    "release": "pnpm prepack && changelogen --release --output ../../CHANGELOG.md  && pnpm publish --no-git-checks --access public && git push --follow-tags",
    "npm-auth-check": "[[ $(npm_config_registry=https://registry.npmjs.org npm whoami) = \"busbud-dev\" ]] && echo \"NPM Authentication ✅\"",
    "prepack": "pnpm build",
    "test:ci": "CI=true pnpm test:unit",
    "test:unit": "vitest"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "changelogen": "^0.5.5",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vite-plugin-dts": "^3.8.1",
    "vitest": "^1.4.0"
  }
}
