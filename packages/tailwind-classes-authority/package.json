{
  "name": "tailwind-classes-authorithy",
  "private": false,
  "version": "0.0.2",
  "type": "module",
  "license": "UNLICENSED",
  "files": [
    "dist"
  ],
  "main": "./dist/tca.umd.cjs",
  "module": "./dist/tca.js",
  "types": "./dist/tca.d.ts",
  "exports": {
    ".": {
      "import": "./dist/tca.js",
      "require": "./dist/tca.umd.cjs",
      "types": "./dist/tca.d.ts"
    }
  },
  "engines": {
    "node": "20.12.2"
  },
  "scripts": {
    "build": "tsc && vite build",
    "release": "pnpm prepack && changelogen --release  && pnpm publish --no-git-checks && git push --follow-tags",
    "npm-auth-check": "[[ $(npm_config_registry=https://registry.npmjs.org npm whoami) = \"busbud-dev\" ]] && echo \"NPM Authentication ✅\"",
    "prepack": "pnpm build",
    "test:ci": "CI=true pnpm test:unit",
    "test:unit": "vitest"

  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwind-merge": "^2.3.0",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vite-plugin-dts": "^3.8.1",
    "vitest": "^1.4.0",
    "changelogen": "^0.5.5"
  },
  "dependencies": {
    "tailwindcss": "^3.4.3"
  }
}
